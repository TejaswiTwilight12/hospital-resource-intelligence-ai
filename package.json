{
  "name": "hospital-resource-intelligence",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build"
,
    "start": "cd backend && npm install && node server.js"
  }
}
